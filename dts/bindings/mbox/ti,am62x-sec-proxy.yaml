description: TI Secure Proxy MAILBOX

compatible: "ti,am62x-sec-proxy"

include: [base.yaml, mailbox-controller.yaml]

properties:
  reg:
    required: true

  interrupts:
    required: true

mbox-cells:
  - channel